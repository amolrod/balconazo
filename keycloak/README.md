# Keycloak Configuration

This directory contains configuration resources for Keycloak, including:
- Realm export files (`realm-export.json`)
- Theme customizations (if any)
- Documentation related to Identity and Access Management (IAM).

See `docs/KEYCLOAK_CONFIG.md` for detailed setup instructions.
